export async function scanWalletAirdrops(address) {
  // Placeholder logic (expand later)
  return {
    wallet: address,
    chain: "Base",
    airdrops: [
      {
        protocol: "Base Early User",
        type: "Token",
        claimable: true,
        estimatedValue: "$120"
      }
    ]
  };
}
